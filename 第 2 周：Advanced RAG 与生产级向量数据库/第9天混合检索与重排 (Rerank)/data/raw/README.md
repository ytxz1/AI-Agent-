# 示例语料目录

把今天要检索的 Markdown、TXT、PDF 转文本结果放在这里。

建议先放入 5-10 篇小文档，内容覆盖：

1. BM25 检索原理。
2. Embedding 检索原理。
3. Hybrid Retrieval 融合策略。
4. Rerank 二阶段排序。
5. LlamaIndex 相关 API 笔记。

运行 demo 前，至少放入一个 `.md` 或 `.txt` 文件。
